# Tree Annotator

A simple GUI to load a point cloud in the .las format, slice it at regular intervals and annotate stem points.
It is built upon laspy, PyQt5, and matplotlib

# Tree Manager Inspector

A simple open3d-based gui that laods a tree manager from a zip file and visualizes all tree clouds.